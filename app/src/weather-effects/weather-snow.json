{
  "vnmWeatherEffect": true,
  "name": "飘雪·多层视差",
  "version": "1.0.0",
  "description": "参考 Shadertoy Just snow 的多层视差雪景：远近雪花、景深柔焦与指针风向偏移，全部使用顺色高光。",
  "params": [
    {"key":"amount","label":"雪量","min":1,"max":10,"default":5,"step":1},
    {"key":"size","label":"雪花大小","min":50,"max":180,"default":100,"step":5,"unit":"%"},
    {"key":"speed","label":"下落速度","min":20,"max":180,"default":70,"step":5,"unit":"%"},
    {"key":"wind","label":"风向","min":-100,"max":100,"default":12,"step":1,"unit":"%"},
    {"key":"softness","label":"景深柔焦","min":0,"max":100,"default":42,"step":1,"unit":"%"}
  ],
  "code": "(function(api){\nvar layers=[];var W=0,H=0;var seed=0;var TAU=6.283185307;\nfunction rnd(){seed=(seed*1664525+1013904223)>>>0;return seed/4294967296;}\nfunction make(w,h){layers=[];seed=94731;var a=api.params&&api.params.amount||5;var count=Math.max(1,Math.min(10,a));var total=count*55;for(var i=0;i<total;i++){var z=rnd();layers.push({x:rnd()*w,y:rnd()*h,r:.7+z*2.8,v:.45+z*1.25,drift:(rnd()-.5)*(.25+z*.8),phase:rnd()*TAU,z:z,tw:rnd()*TAU});}}\nfunction resize(w,h){W=w;H=h;if(W&&H)make(W,H);}\nfunction frame(dt){W=api.width;H=api.height;if(!W||!H)return;var p=api.params||{};var amount=+p.amount||5;var scale=(+p.size||100)*.01;var speed=(+p.speed||70)*.01;var wind=(+p.wind||0)*.01;var soft=(+p.softness||0)*.01;var f=Math.min(3,Math.max(0,dt/16.6));if(!layers.length||layers.length!==Math.max(1,Math.min(10,amount))*55)make(W,H);var pt=api.pointer||{};var pan=pt.x?((pt.x/W)-.5)*18:0;var ctx=api.ctx;ctx.clearRect(0,0,W,H);ctx.save();ctx.globalCompositeOperation='source-over';for(var i=0;i<layers.length;i++){var s=layers[i];var depth=.25+s.z*.75;s.y+=(s.v*speed*(.7+s.z*1.1))*f;s.x+=(wind*(.8+s.z*1.4)+s.drift+Math.sin((api.now()*.001)+s.phase)*.08)*f;if(s.y>H+8){s.y=-8;s.x=rnd()*W;}if(s.x>W+12)s.x=-12;if(s.x<-12)s.x=W+12;var x=s.x+pan*depth;var r=s.r*scale*(.55+s.z*.9);var alpha=(.2+.65*s.z)*(.85+.15*Math.sin(api.now()*.002+s.tw));ctx.globalAlpha=Math.max(0,Math.min(.95,alpha));if(soft>0){ctx.shadowBlur=soft*(1-s.z)*4;ctx.shadowColor='rgba(255,255,255,'+(.35*ctx.globalAlpha)+')';}else{ctx.shadowBlur=0;}ctx.fillStyle='rgba(255,255,255,1)';ctx.beginPath();ctx.arc(x,s.y,r,0,TAU);ctx.fill();if(s.z>.72){ctx.globalAlpha*=.35;ctx.strokeStyle='rgba(255,255,255,1)';ctx.lineWidth=Math.max(.35,r*.18);ctx.beginPath();ctx.moveTo(x-r*1.5,s.y);ctx.lineTo(x+r*1.5,s.y);ctx.moveTo(x,s.y-r*1.5);ctx.lineTo(x,s.y+r*1.5);ctx.stroke();}}ctx.restore();}\nfunction clear(){layers=[];try{api.ctx.clearRect(0,0,api.width,api.height);}catch(e){}}\nreturn{frame:frame,resize:resize,clear:clear};\n})"
}
